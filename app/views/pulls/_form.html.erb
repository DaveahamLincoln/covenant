<%= simple_form_for @pull do |f| %>
    <% if @pull.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@pull.errors.count, "error") %> prohibited this pull from being saved:</h2>
  
        <ul>
        <% @pull.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>